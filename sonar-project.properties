# SonarCloud configuration for Next.js project
sonar.projectKey=C11R11_laboratorio-final-devsecops
sonar.organization=c11r11

# Source code
sonar.sources=src
# Tests (only if you have actual test files in a separate directory)
# sonar.tests=tests

# Exclude patterns
sonar.exclusions=**/node_modules/**,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,**/__tests__/**,**/coverage/**,**/.next/**,**/out/**,**/build/**,**/dist/**

# Test coverage (if you have coverage reports)
# sonar.javascript.lcov.reportPaths=coverage/lcov.info

# TypeScript configuration
sonar.typescript.tsconfigPath=tsconfig.json

# Language
sonar.language=ts

# Encoding
sonar.sourceEncoding=UTF-8 